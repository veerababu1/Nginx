import { RestrictedCharactersDirective } from './restricted-characters.directive';

describe('RestrictedCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
